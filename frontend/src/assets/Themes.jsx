import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#51514f',
    },
  },
  typography: {
    fontFamily: 'Lora',
  },
});

export default theme;
