import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#51514f',
    },
    secondary: {
      main: '#342d2b',
    },
  },
  typography: {
    fontFamily: 'Lora',
  },
});

export default theme;
